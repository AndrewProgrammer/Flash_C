Ќаписать класс Exception с подвидами. (¬нимательно написать так как там мы будем писать описание ошибок)
try,catch написать в main.
Public: об€зательно проверить на аргументы.
ƒл€ решени€ проблемы с исключени€ми, мы должны написать примерно так,совет нужно правильно поставить исключени€( не может быть объект nullptr):
try
{
	unique_ptr<char> * s = new char[20];
	cin >> s;
	String str(str);
	delete[] s;//Ќе сработает, поэтому нужно написать тип данных unique_ptr;
}