 онстантный метод - это метод который можно вызвать от константного объекта.
const String str("Hello")
cout << str.GetLength();
ƒл€ того чтобы сделать метод константный нужно в конце сигнатуры дописать слово const.
Ќапример:
uint GetLength(); 
uint GetLength() const
{
	this - в константных методов: это констатный указатель на констатную переменную. Ќапример: const String* const this
	this-> m_length = 0;//ошибка, нельз€ изменить значени€ полей класса в константных методах.ћожно их только считовать.
	ћожно вызывать другие только константные методы внутри константных методов.
	—татически методы не могут быть константные.
	 онстантный метод не может быть конструктором и деструктором.
}
ћожно перегружать константные и не константные методы.
int main()
{
	String s("Hello");
	F(s);
}
void F(const String& x)
{
	cout << x.GetLength();	
}
mutable пол€ - можно мен€ть внутри константных методов.
immutable - означает что объект после своего создани€ никогда не мен€етс€.